# CERT Python Secure Coding Standard

This document provides guidelines for secure Python programming. Topics include avoiding eval() and exec(), secure file handling, safely parsing XML (avoiding XXE), and using parameterized SQL queries to prevent SQL injection.